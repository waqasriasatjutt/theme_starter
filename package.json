{
  "name": "theme-starter",
  "version": "1.1.0",
  "private": true,
  "description": "A Websites Portal theme written entirely in React (JSX). `components/sections/*.jsx` are the source of truth; `npm run build:sections` renders them to `sections/*` which the portal imports as editable drag-drop blocks.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "build:sections": "node scripts/build-sections.mjs"
  },
  "dependencies": {
    "next": "14.2.15",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.23.1"
  }
}
